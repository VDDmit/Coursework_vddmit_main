spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/task_tracker_db
    username: postgres
    password: 12341234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect